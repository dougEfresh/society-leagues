
/*
jQuery(document).ready(function ($) {

    login($);
    accountInfo($);
    teamHistory($);
    //Stupid ajax calls don't make request in the order I specify, so I have to call it twice to get the token
    //eraseCookie('X-Auth-Token');
    login($);
    accountInfo($);
    teamHistory($);
    console.log('Token in Jquery: '+ window.XauthToken);
});

    */